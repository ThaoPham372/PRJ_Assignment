<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">
         
    <display-name>Gym Management System</display-name>
    <description>Comprehensive Gym Management Application</description>
    
    <!-- Servlet definitions -->
    <servlet>
        <servlet-name>HomeServlet</servlet-name>
        <servlet-class>com.gym.controller.HomeServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>
    
    <servlet-mapping>
        <servlet-name>HomeServlet</servlet-name>
        <url-pattern>/home</url-pattern>
    </servlet-mapping>
    
    <!-- Other servlets use @WebServlet annotations (no explicit declarations needed) -->
    
    <!-- Welcome file list -->
    <welcome-file-list>
        <welcome-file>home.jsp</welcome-file>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>
    
    <!-- Session timeout (30 minutes) -->
    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>
    
    <!-- Error pages -->
    <error-page>
        <error-code>404</error-code>
        <location>/views/error/404.jsp</location>
    </error-page>
    
    <error-page>
        <error-code>500</error-code>
        <location>/views/error/500.jsp</location>
    </error-page>
    
    <!-- Character encoding filter -->
    <filter>
        <filter-name>CharacterEncodingFilter</filter-name>
        <filter-class>com.gym.filter.CharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
    </filter>
    
    <filter-mapping>
        <filter-name>CharacterEncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>
    
    <!-- Dev bypass auth filter (DEV ONLY) -->
    <filter>
        <filter-name>DevBypassAuthFilter</filter-name>
        <filter-class>com.gym.filter.DevBypassAuthFilter</filter-class>
    </filter>

    <!-- Apply bypass to PT-related routes so we can access without login -->
    <filter-mapping>
        <filter-name>DevBypassAuthFilter</filter-name>
        <url-pattern>/pt/*</url-pattern>
    </filter-mapping>
    <!-- Remove nonexistent mappings; rely on annotations -->
    <filter-mapping>
        <filter-name>DevBypassAuthFilter</filter-name>
        <url-pattern>/ScheduleServlet</url-pattern>
    </filter-mapping>
    
    <!-- Security constraints sẽ được thêm sau khi có authentication -->
    
</web-app>
